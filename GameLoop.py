class GameLoop:
    
    import numpy as np

    def gameloop(next_square:int, game_array):
        if (game_array[next_square]!=1 and game_array[next_square]!=-1)
        space=input("Which space would you like to play \n1, 2, 3\n4, 5, 6\n7, 8, 9\n")
        game_array[next_square,space];
